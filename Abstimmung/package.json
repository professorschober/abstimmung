{
  "name": "abstimmung",
  "version": "0.1.0",
  "private": true,
  "description": "PWA for classroom quizzes and live voting.",
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev --workspace server\" \"npm run dev --workspace client\"",
    "build": "npm run build --workspace server && npm run build --workspace client",
    "test": "npm run typecheck --workspace server && npm run typecheck --workspace client",
    "build:server:azure": "esbuild ././server/src/index.ts --bundle --platform=node --format=cjs --target=node24 --outfile=.azure-deploy/bundle/index.cjs"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "esbuild": "^0.28.1"
  }
}
